<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Styles required by the homescreen widget layout -->
    <style name="Widget.Android.AppWidget.Container">
        <item name="android:padding">8dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/white</item>
    </style>

    <!-- Use a platform theme parent to avoid requiring MaterialComponents dependency -->
    <style name="Theme.Android.AppWidgetContainer" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:textColorPrimary">#000000</item>
    </style>

    <style name="Widget.Android.AppWidget.InnerView">
        <item name="android:padding">4dp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Minimal base style to satisfy references from widget/layout libraries -->
    <style name="Widget.Android.AppWidget">
        <!-- Intentionally minimal; extend if you need default attributes -->
        <item name="android:padding">0dp</item>
    </style>

    <!-- Minimal parent style referenced by other widget styles -->
    <style name="Widget.Android">
        <item name="android:padding">0dp</item>
    </style>

    <!-- Minimal global Widget style to satisfy library references -->
    <style name="Widget" />
</resources>
