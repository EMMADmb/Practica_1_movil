name: shrine
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # --- SECCIÓN CORREGIDA ---
  # 'flutter:' no tiene espacios antes.
  # 'assets:' tiene dos espacios antes.
  # '- assets/' tiene dos espacios antes y un guion.
  assets:
    - assets/

